/**
 * replace method
 * 
 * @param {String} str 
 * @param {RegExp} what 
 * @param {String} replacement 
 * @returns {String}
 */
export default replace = (str, what, replacement) => str.replace(what, replacement);