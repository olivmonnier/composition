{
  "name": "composition",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "babelify": "browserify src/main.js -o dist/bundle.js -t [ babelify ]",
    "uglify": "uglifyjs dist/bundle.js -o dist/bundle.min.js -c",
    "test": "mocha --compilers js:babel-core/register --reporter spec --timeout 5000 --recursive",
    "doc": "jsdoc ./src -r -d docs",
    "build": "npm run babelify && npm run uglify"
  },
  "keywords": [],
  "author": "olivmonnier",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-2": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^13.1.1",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "uglify-js": "^2.7.4"
  },
  "dependencies": {
    "jsdoc": "^3.4.3"
  }
}
